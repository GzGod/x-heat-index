__pycache__/
*.pyc
*.pyo
.venv/
venv/
.DS_Store
*.swp
*.bak
*.tmp

# Local data — never commit (contains tweet content + author handles)
data/

# Local env override (should never have real secrets)
.env

# Claim-runtime artifacts (per-clone, not source-controlled)
.claim-proof.log
.claude/settings.local.json
